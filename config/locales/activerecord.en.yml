---
en:
  activerecord:
    attributes:
      poll:
        expires_at: Deadline
        options: Choices
      user:
        agreement: Service agreement
        email: E-mail address
        locale: Locale
        password: Password
      user/account:
        username: Username
      user/invite_request:
        text: Reason
      oauth_authentication:
        provider: Provider
        uid: Twitter ID
        username: Account Name
    errors:
      models:
        account:
          attributes:
            username:
              invalid: must contain only letters, numbers and underscores
              reserved: is reserved
        status:
          attributes:
            reblog:
              taken: of status already exists
        user:
          attributes:
            email:
              blocked: uses a disallowed e-mail provider
              unreachable: does not seem to exist
        oauth_authentication:
          attributes:
            base:
              enabled_two_factor_authentication: You can not link twitter becouse two-factor authentication is enabled.
            uid:
              taken: has already been taken
